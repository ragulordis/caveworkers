import { z } from "zod";
import { protectedProcedure, router } from "../_core/trpc";
import { createQaDefectForUser, createQaPlanForUser, createQaRegressionCollaborationForUser, getQaDashboardForUser, getQaMemoryForUser, recordTestRunForUser, storeQaSharedMemoryForUser, verifyQaDefectForUser } from "../qa/service";
const testType = z.enum(["unit", "integration", "api", "ui", "end_to_end", "regression", "smoke", "performance", "security_regression"]);
export const qaRouter = router({ dashboard: protectedProcedure.query(({ ctx }) => getQaDashboardForUser(ctx.user.id)), memory: protectedProcedure.query(({ ctx }) => getQaMemoryForUser(ctx.user.id)), createPlan: protectedProcedure.input(z.object({ title: z.string().min(3).max(255), featureDescription: z.string().min(3).max(10_000), riskLevel: z.enum(["low", "medium", "high", "critical"]), testTypes: z.array(testType).min(1) })).mutation(({ ctx, input }) => createQaPlanForUser(ctx.user.id, input)), recordRun: protectedProcedure.input(z.object({ planId: z.number().int().positive().optional(), taskId: z.number().int().positive().optional(), toolName: z.string().min(2).max(120), suiteName: z.string().min(2).max(255), environment: z.string().min(2).max(100), total: z.number().int().min(0), passed: z.number().int().min(0), failed: z.number().int().min(0), skipped: z.number().int().min(0), durationSeconds: z.number().int().min(0), riskLevel: z.enum(["low", "medium", "high", "critical"]), summary: z.string().max(4_000).optional() })).mutation(({ ctx, input }) => recordTestRunForUser(ctx.user.id, input)), createDefect: protectedProcedure.input(z.object({ runId: z.number().int().positive().optional(), taskId: z.number().int().positive().optional(), title: z.string().min(3).max(255), severity: z.enum(["blocker", "critical", "high", "medium", "low"]), environment: z.string().min(2).max(100), stepsToReproduce: z.string().min(3).max(10_000), expectedResult: z.string().min(3).max(10_000), actualResult: z.string().min(3).max(10_000), evidence: z.string().min(3).max(10_000) })).mutation(({ ctx, input }) => createQaDefectForUser(ctx.user.id, input)), verifyDefect: protectedProcedure.input(z.object({ defectId: z.number().int().positive(), runId: z.number().int().positive().optional(), summary: z.string().min(3).max(10_000), status: z.enum(["verified", "failed", "blocked"]) })).mutation(({ ctx, input }) => verifyQaDefectForUser(ctx.user.id, input)), storeSharedMemory: protectedProcedure.input(z.object({ category: z.enum(["testing_standards", "release_workflow", "quality_risks", "critical_user_flows"]), title: z.string().min(3).max(180), value: z.string().min(3).max(10_000) })).mutation(({ ctx, input }) => storeQaSharedMemoryForUser(ctx.user.id, input)), createRegressionCollaboration: protectedProcedure.input(z.object({ teammate: z.enum(["cybersecurity-analyst", "data-analyst"]), title: z.string().min(3).max(255), evidence: z.string().min(3).max(10_000), relatedTaskId: z.number().int().positive().optional() })).mutation(({ ctx, input }) => createQaRegressionCollaborationForUser(ctx.user.id, input)) });
