import { AlertTriangle, FileSearch, Radar, ShieldCheck } from "lucide-react";
import { trpc } from "@/lib/trpc";

export default function SecurityEmployeePanel() {
  const dashboard = trpc.security.dashboard.useQuery();
  const data = dashboard.data;
  if (!data) return <div className="grid min-h-64 place-items-center text-xs text-zinc-600">Loading Maya’s security state…</div>;
  return <div><div className="flex items-center justify-between"><p className="mono text-[10px] uppercase tracking-[.14em] text-zinc-600">Employee</p><span className="mono text-[9px] text-emerald-300">SECURITY</span></div><div className="mt-4"><div className="flex items-center gap-3"><div className="relative grid size-12 place-items-center rounded-2xl bg-emerald-400/12 text-emerald-200"><ShieldCheck className="size-5" /><span className="status-pulse absolute -right-0.5 -top-0.5 size-3 rounded-full border-2 border-[#15161a] bg-emerald-400" /></div><div><h2 className="text-[15px] font-semibold text-white">{data.analyst.name}</h2><p className="mt-0.5 text-xs text-zinc-500">{data.analyst.role}</p></div></div><div className="mt-3 inline-flex items-center gap-1.5 rounded-full border border-emerald-300/15 bg-emerald-300/10 px-2 py-1 text-[10px] font-semibold text-emerald-200"><span className="size-1.5 rounded-full bg-current" />{data.analyst.status}</div><p className="mt-3 text-xs leading-5 text-zinc-500">Evidence-led review, bounded tooling, and human escalation for sensitive operations.</p></div><div className="my-5 border-t border-white/[.065]" /><section><p className="text-xs font-medium text-zinc-300">Current review</p><div className="mt-3 rounded-xl border border-emerald-300/[.13] bg-emerald-400/[.06] p-3"><div className="flex items-start gap-2"><Radar className="mt-0.5 size-3.5 text-emerald-300" /><div><p className="text-[11px] font-medium leading-4 text-zinc-200">{data.analyst.currentTask}</p><p className="mt-1 text-[10px] text-zinc-600">{data.activeReview?.status ?? "Monitoring"}</p></div></div></div></section><div className="my-5 border-t border-white/[.065]" /><section><p className="text-xs font-medium text-zinc-300">Security posture</p><div className="mt-3 grid grid-cols-2 gap-2"><div className="rounded-xl bg-white/[.035] p-3"><p className="mono text-[9px] uppercase tracking-[.1em] text-zinc-600">Risk level</p><p className="mt-1 text-sm font-semibold text-amber-200">{data.analyst.riskLevel}</p></div><div className="rounded-xl bg-white/[.035] p-3"><p className="mono text-[9px] uppercase tracking-[.1em] text-zinc-600">Risk score</p><p className="mt-1 text-sm font-semibold text-zinc-200">{data.riskScore}/100</p></div></div></section><div className="my-5 border-t border-white/[.065]" /><section><div className="flex items-center gap-2"><AlertTriangle className="size-3.5 text-amber-200" /><p className="text-xs font-medium text-zinc-300">Latest finding</p></div><div className="mt-3 rounded-xl bg-white/[.03] p-3"><p className="text-[11px] leading-4 text-zinc-300">{data.analyst.latestFinding}</p><p className="mt-1.5 text-[10px] text-zinc-600">Evidence is recorded in the security review.</p></div></section><div className="my-5 border-t border-white/[.065]" /><section><div className="flex items-center gap-2"><FileSearch className="size-3.5 text-violet-300" /><p className="text-xs font-medium text-zinc-300">Boundary</p></div><p className="mt-2 text-[11px] leading-5 text-zinc-500">Production changes, credential actions, and expansive scanning require explicit approval.</p></section></div>;
}
